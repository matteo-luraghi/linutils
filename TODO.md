# TODO

- Rust crates.io
- Specific profiles via in-line arguments (ex, for server config, laptop, ...)
- Smaller panel for distros, split packages in different columns for ease of use
- Modularise TUI into a "package" like structure (ex, package TUI with modules for states)
- Add distro support
- Search for packages or press a letter of the alphabet and jump to the first in the list
